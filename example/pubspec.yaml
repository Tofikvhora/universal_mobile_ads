name: universal_mobile_ads_example
description: "Demonstrates how to use the universal_mobile_ads package."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  universal_mobile_ads:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
